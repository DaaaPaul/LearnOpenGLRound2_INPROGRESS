#version 330

in vec3 color;
in vec2 textureCoordinate;
out vec4 fragmentColor;

uniform sampler2D awesomeface;
uniform sampler2D container;

void main() {
	fragmentColor = mix(texture(awesomeface, textureCoordinate), texture(container, textureCoordinate), 0.5);
}